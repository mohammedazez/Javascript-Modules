// Method mengucapkan selamat datang kepada user

export function hallo(user) {
  alert("halo selamat datang" + " " + user);
}

export function thankYou(nama, produk) {
  alert("Terimakasih " + nama + "Sudah membeli " + produk);
}
